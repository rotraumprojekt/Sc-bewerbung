<!DOCTYPE html>
<html lang="de">
<head>
  <title><%= edition.name %> Community — Rotraum</title>
  <%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/header') %>
  <main class="container">
    <h1><%= edition.name %> — Community</h1>
    <p class="lead">Willkommen, <%= viewer.name %>. Hier sind die anderen Gäste, die für diese Edition angenommen wurden.</p>

    <% if (members.length === 0) { %>
      <p class="muted">Es wurden noch keine anderen Mitglieder angenommen — schau bald wieder vorbei.</p>
    <% } else { %>
      <div class="community-grid">
        <% members.forEach(function(m) { %>
          <div class="community-card">
            <% if (m.photo) { %><img src="/uploads/<%= m.photo %>" alt="<%= m.name %>"><% } %>
            <h3><%= m.name %></h3>
            <% if (m.instagram) { %><a href="<%= instagramUrl(m.instagram) %>" target="_blank" rel="noopener">Instagram →</a><% } %>
          </div>
        <% }) %>
      </div>
    <% } %>
  </main>
  <%- include('../partials/footer') %>
</body>
</html>
